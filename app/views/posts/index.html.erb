<div class="row">
  <% for post in @posts %>
    <div class="col-3">
      <h4>
        <div>
         <p><%= post["body"] %></p>
         <img src="<%= post["image"] %>" />
         <p>Posted by: <%= post.user ? post.user["first_name"] : "Anonymous" %></p>
        </div>
        anonymous
      </h4>
      <img src="<%= post["image"] %>" class="img-fluid w-full">
      <p><%= post["body"] %></p>
    </div>
  <% end %>
